class Calcul (val nu: MutableList<MutableList<Double>>) {
    var answer = arrayOf<String>()
    var fifteenChecker = false
    init {
        caculate(nu)
    }
    fun caculate(aList: MutableList<MutableList<Double>>) {

        for (i in 0 until aList.size) {
            when {

            }

        }

    }
}